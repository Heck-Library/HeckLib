{
    "name": "hecklib",
    "author": "Jevk",
    "license": "MIT",
    "version": "3.1.1",
    "description": "A modular TypeScript library for Beat Saber modmapping using only Node.js",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "files": [
        "dist"
    ],
    "scripts": {
        "build": "tsc",
        "test": "npx node ./script.js",
        "buildTest": "call npm run build && call npm run test",
        "watch": "npx nodemon -e ts --exec \"call npm run buildTest\"",
        "clean": "tsc --build --clean",
        "cleanBuild": "call npm run clean && call npm run build"
    },
    "devDependencies": {
        "nodemon": "^3.1.9",
        "ts-node": "^10.9.2",
        "typescript": "^5.0.0"
    },
    "dependencies": {
        "@types/node": "^22.13.14"
    }
}
